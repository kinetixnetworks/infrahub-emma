# NOTE: This workflow was originally used by OpsMill to sync docs to opsmill/infrahub-docs.
# It is disabled in this fork as we do not have access to that repository.
# To re-enable, update the target repository and PAT_TOKEN secret.
name: Sync Folders

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Doc sync is disabled in this fork. Update the workflow to target your own docs repo."
